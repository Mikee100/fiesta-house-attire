// Legacy path kept for backward-compat imports — the real implementation lives in
// components/reviews/ (ReviewsWidget.tsx, ReviewCard.tsx, reviews-data.ts).
export { default } from "@/components/reviews/ReviewsWidget";

